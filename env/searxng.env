# Environment variables for SearXNG service

# Hostname for SearXNG (from original .env)
SEARXNG_HOSTNAME=localhost:8080/

# Number of uWSGI workers (from original .env)
# SEARXNG_UWSGI_WORKERS=4

# Number of uWSGI threads per worker (from original .env)
# SEARXNG_UWSGI_THREADS=4

# Redis connection URL (overrides settings.yml)
SEARXNG_REDIS_URL=redis://redis:6379/0